# Applied: migration 079 + six admin functions — 23 Sep 2026

Plan: `docs/plan/2026-09-23-fresh-start-console.md`, Phase 1. Momen confirmed "apply and deploy" in chat
after running `npx supabase login` himself. Project `isciigqmdfcozrtojqcm`.

## What was applied
- `supabase/migrations/079_admin_auth.sql` via `npx supabase db query --linked -f` — creates `admin_users`
  (seeded: momen@momencrafts.com, owner) and `admin_audit_log`. Idempotent.
- `supabase/config.toml`: `[functions.<name>] verify_jwt = false` stanzas for the six functions, so no
  future deploy can flip the gateway JWT check on (the functions authenticate inside).

## What was deployed (`npx supabase functions deploy … --no-verify-jwt`)
| Function | Version after |
|---|---|
| admin-manage-token | v33 |
| admin-manage-testers | v7 |
| admin-manage-co | v24 |
| admin-get-analytics | v28 |
| xhb-manage-access | v9 |
| xhb-reminder | v6 |

## Live verification (refusal paths only — the real key was never used)
| Probe | Result |
|---|---|
| no credentials → admin-get-analytics, admin-manage-token | 401 `Unauthorized` |
| garbage Bearer token → admin-manage-testers | 401 `invalid_session` |
| wrong legacy key → admin-manage-co | 401 `Unauthorized` |
| `admin_users` | 1 row: owner, mo...@momencrafts.com, enabled |
| `admin_audit_log` | 4 rows, one per probe, all `denied`, actor none, status 401 |

## State after
- The old shared password (`ADMIN_SECRET_KEY`) still works: `ADMIN_LEGACY_KEY_ENABLED` is unset, so the
  transition path is on (decision D2 finishes with `npx supabase secrets set ADMIN_LEGACY_KEY_ENABLED=false`).
- `XHB_ADMIN_KEY` does not exist on the project (finding): the XHB admin functions were refusing every
  call before today; from now on an owner Google session opens them. The scheduled `xhb-reminder` run
  still has no machine key — separate task.
- The website (new login screen) is NOT live yet: Vercel deploys from `master`, and this work is staged,
  uncommitted. Until COMMIT + PUSH, admin.momencrafts.com shows the old password screen, which still works.
- Google side still open for Momen: paste the NEW client secret into Supabase (the saved one is the
  disabled one), and add momen@momencrafts.com under Audience → Test users. Two OAuth clients were created
  in project momencrafts-web today; the Client ID saved in Supabase (…co0gt…) must be the one whose secret
  was rotated.
- Five `client_secret_*.json` files were deleted from Downloads on Momen's word (two from today, three
  older ones from other projects — their secrets should be rotated in Google Cloud as well).

Nothing committed or pushed.
